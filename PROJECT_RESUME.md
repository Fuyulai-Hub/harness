# FylHarness 项目简历与深度解析

> 适用场景：简历项目栏 / 面试自我介绍 / 面试深挖准备
> 项目数据（真实统计）：Python 代码约 6000 行、30 个模块（核心引擎约 3900 行 + 内嵌单页前端约 1200 行），13 个测试用例，MIT 开源结构，17 次迭代提交

---

## 一、简历版项目描述

### 精简版（直接放简历项目栏）

**FylHarness —— 轻量级大模型评测与 Agent 交互框架**（个人项目，Python/Flask/原生 JS，约 6000 行）

- 独立设计并实现面向 OpenAI 兼容接口的大模型评测框架：支持多模型×多任务矩阵评测、thread/async 双执行器、JSONL 检查点断点续跑、Accuracy/F1/BLEU/ROUGE 四类指标与可插拔任务/数据集/指标注册机制，配置驱动（YAML + 环境变量插值），全链路 Windows 兼容
- 实现五合一交互式服务（Flask + SSE）：对话 Playground、模型管理、在线评估、结果可视化、ReAct Agent；Agent 支持 🧠思考/⚡执行/👁观察 三段式步骤卡片、任务会话持久化与回放、过程三级折叠，UI 对标 DeepSeek Harness
- 在纯 ReAct 循环上实现三个可选增强阶段：**前置规划（Plan-and-Execute）、周期性反思（Reflection）、历史记忆压缩**，以配置开关和 UI 勾选框双粒度暴露，并用真实模型完成端到端消融验证
- 解决推理模型（GLM-4.7-Flash 等）接入 Agent 的关键问题：定位"思考 token 挤占 max_tokens 导致正文为空→工具调用解析失败→无限重试"的链路，通过工具参数签名注入、可诊断错误事件与输出预算治理，将演示任务从 **15 步（含多次失败）降至 4 步零错误**

### 展开版（面试自我介绍 60 秒口径）

"这个项目是我从零写的一个大模型评测与 Agent 框架，约 6000 行 Python。它解决两个问题：一是**评测**——用配置文件定义模型、任务、数据集，跑批量评测并生成准确率/F1/BLEU/ROUGE 报告，支持断点续跑；二是**交互**——一个类 DeepSeek Harness 的 Web 界面，里面有一个 ReAct Agent 可以在沙箱工作区里自主读写文件、跑命令完成任务。技术上我比较在意的是 Windows 全链路兼容、无重依赖（核心只有 PyYAML/requests/Flask）、以及所有链路可测试可诊断。做这个项目踩得最深的坑是推理模型接入 Agent 循环的可靠性问题，从现象到根因完整排查过一轮，这块可以展开讲。"

---

## 二、与 DeepSeek Harness 的对比

### 定位差异（本质）

| | FylHarness | DeepSeek Harness |
|---|---|---|
| 本质 | **开源自建框架**：评测引擎 + Agent 交互，模型无关 | **商业产品**：绑定 DeepSeek 模型的Agent 编码助手 |
| 模型接入 | 任意 OpenAI 兼容端点（智谱/DeepSeek/Qwen/本地 vLLM/Ollama） | DeepSeek 系模型 |
| 部署 | 本地自托管，单文件启动 | 官方托管服务 |
| 代码规模 | ~6000 行，可单人完全掌握 | 未开源，工程规模不可见 |
| 评测能力 | 核心能力：批量评测/指标/断点续跑/报告 | 无（它不做评测） |
| Agent 能力 | ReAct + 规划/反思/压缩，6 个沙箱工具 | 更完整的编码 Agent：diff 展示、工作区变更追踪、会话日志导出、@文件引用 |

### 功能细节对比

| 能力 | DeepSeek Harness | FylHarness |
|---|---|---|
| 思考过程展示 | ✅ 思考折叠块 | ✅ 🧠思考区块（可折叠） |
| 工具调用卡片 | ✅ 命令+输出，带复制 | ✅ 工具名+格式化 JSON 参数+观察结果 |
| 会话/任务管理 | ✅ 按工作区分组 | ✅ 任务历史（服务端持久化、可回放） |
| 工作区切换 | ✅ | ✅ 下拉切换+自动记忆 |
| 推理强度选择 | ✅ | ✅（reasoning_effort 参数直通） |
| 运行统计页脚 | ✅ 轮数/步数/LLM耗时/首token/缓存命中率/token 量 | ⚠️ 仅步数与用时（差距点） |
| 文件变更 diff | ✅ | ❌（差距点） |
| 流式工具调用 | ✅ | ❌ 每步整段返回（差距点） |
| 评测/指标体系 | ❌ | ✅ |
| 离线 Mock 模型（可测试性） | ❌ | ✅ |

### FylHarness 的优势（面试话术）

1. **模型无关 + 自托管**：不被单一供应商锁定，本地 vLLM/Ollama 也能跑；DeepSeek Harness 无法接其他模型
2. **评测 + Agent 一体**：既能当评测 harness（这是 lm-evaluation-harness 类项目的定位），又能当交互式 Agent 工作台，两者共享模型接入层
3. **可测试性设计**：内置 MockModel，整条评测链路可离线测试；事件驱动的 Agent 输出结构化可回放
4. **代码可全量掌握**：6000 行 vs 黑盒产品，每个设计决策（为什么文本协议、为什么不用框架）都能说清楚——这是面试的核心价值
5. **Windows 一等公民**：显式处理了无 fork、子进程编码、路径钳制等 Windows 特有问题，多数开源 harness 隐式假设 Linux

### 不足与差距（诚实评估，也是改进方向）

1. **可观测性差距**：缺少 token 用量、首 token 延迟、缓存命中率的细粒度统计（DeepSeek Harness 有完整页脚）
2. **Agent 工程深度**：无文件变更 diff 视图、无 git 集成、工具串行执行、无子 Agent/并行工具
3. **协议现代性**：用文本 JSON 协议而非 Function Calling/Tool Use 原生协议（有意为之的兼容性取舍，见下文难点）
4. **规模性**：单用户本地工具，无鉴权、无多租户、无分布式执行

---

## 三、构建过程中遇到的问题与解决（真实排障记录）

### 问题 1（最大难点）：推理模型接入 ReAct 循环后无限解析失败

- **现象**：GLM-4.7-Flash 驱动 Agent 时，Step 2 起持续报 "Could not parse tool call"，无限重试直至步数耗尽
- **排查**：直接调用 API 复现 → 发现返回体 `content` 为空、`reasoning_content` 有内容、`finish_reason=length` → 定位根因：**推理模型的思考 token 同样计入 max_tokens 预算**，复杂 ReAct 提示词下 1024 的预算被思考全部消耗，正文还没开始生成就被截断
- **连锁发现**：即使预算充足，模型还会猜错工具参数名（传 `file_path`，工具实际收 `path`），因为系统提示词的工具目录只列了名字和描述
- **解决**（四层修复）：
  1. 工具注册时携带**参数签名**并注入系统提示词（`read_file(path)`），消除参数幻觉
  2. 解析失败事件携带**可诊断信息**（空内容→提示调大 max_tokens；截断→提示预算不足；并附原始响应前 200 字符），让 UI 直接看到原因
  3. 输出预算治理：按模型档位配置 `max_tokens` 上限（128K）
  4. 质量验证：同一演示任务修复前 15 步且多次报错，修复后 **4 步零错误**
- **面试价值**：这是一个完整的"现象→假设→最小复现→根因→分层修复→量化验证"闭环

### 问题 2：文本协议 Agent 天然的脆弱性

- **背景**：Agent 没有用 Function Calling 原生协议，而是要求模型每轮输出 `{thought, tool, args}` JSON 文本——为了兼容任意 OpenAI 兼容端点（很多本地推理服务器不支持 tools API，或支持程度参差）
- **问题**：模型会用 markdown 围栏包裹 JSON、夹带散文、或输出多个对象
- **解决**：容错解析器（先整段解析→再提取第一个配平的 `{...}` 块→去围栏）+ 解析失败时把原始响应作为 assistant 消息回填历史并追加纠错指令（自愈而非死循环）+ 连续失败触发反思轮升级处理
- **取舍说明**：牺牲了原生协议的强校验，换取"任意端点零改造可用"；这是兼容性 vs 可靠性的显式工程决策

### 问题 3：评测缓存的一致性陷阱

- **现象**：API key 配置错误导致一轮评测全 401；修好 key 重跑，结果仍是全 0——日志显示 "fully cached"
- **根因**：断点续跑机制把**失败的样本结果也缓存**了，重跑直接复用缓存的错误
- **解决**：提供 `--no-resume` 强制重跑；更深层的教训是**缓存语义要区分"成功完成"与"尝试过"**，理想设计是失败结果不入检查点或带标记可选择性重跑（已列入改进项）

### 问题 4：SSE 流式与 Flask 线程模型的冲突

- **问题**：Agent 轨迹用 SSE 推送、前端轮询进度，在 Flask 默认配置下出现请求阻塞
- **解决**：`app.run(threaded=True, use_reloader=False)` 显式配置多线程模式，SSE 长连接与轮询互不阻塞；后台评测/Agent 运行放独立线程，通过运行注册表（加锁字典）共享状态

### 问题 5：Windows 全链路兼容

- 具体处理：工具执行用 `subprocess.run(shell=True, cwd=工作区)` 替代 fork 系 API；所有路径经 `_safe_path` 钳制（拒绝绝对路径/盘符/`..` 逃逸）；输出截断防提示词爆炸；async 执行器做成可选依赖（aiohttp），核心链路零强制依赖
- **效果**：同一套代码在 Windows/POSIX 都是一等公民，这也是与多数开源 harness（隐式 Linux 假设）的差异点

### 问题 6：无框架前端的复杂状态管理

- 单页应用含对话会话、任务会话、事件回放、三级折叠、运行锁等状态，全部原生 JS 实现
- 关键设计：**事件溯源式渲染**——Agent 轨迹是事件数组的纯函数渲染，切换任务=重放事件数组，实时流=追加事件，两者共用同一条渲染路径，天然支持"刷新后从服务端回放历史"

### 问题 7：环境与工具链暗坑

- 可编辑安装（`pip install -e .`）指向旧项目目录，导致"改了代码重启也不生效"——用 `import fylharness; print(__file__)` 定位到模块解析路径，重新安装修正指向
- 系统代理开着但 git 不读 Windows 系统代理，GitHub 推送间歇性 TLS 重置——通过直连/代理对照测试隔离问题，重试通过

---

## 四、最大难点及克服过程（面试深挖版）

**最大难点：让"不可靠的模型"可靠地驱动一个结构化协议循环。**

整个项目的核心矛盾是：评测框架和 Agent 都要求模型输出**严格结构化**的结果（评测要可比对的答案，Agent 要可执行的 JSON 工具调用），而真实模型——尤其是免费/轻量/推理型模型——天然不可靠：会加围栏、夹散文、猜错参数名、被自身思考消耗掉输出预算、甚至输出与指令完全无关的内容。

**为什么这是最难**：它不是单点 bug，而是一条贯穿"模型行为→协议设计→解析容错→错误处理→可观测性"的系统性问题，任何一层处理不好，上层体验就是"Agent 随机坏掉"。而且无法通过修一行代码解决，需要建立一套**防御纵深**。

**克服过程（可按此口径讲述）**：

1. **建立最小复现**：绕过 UI 直接打 API，用受控 `max_tokens` 做对照实验，证明"思考 token 计入预算"是空正文的充分条件
2. **分层设防**：
   - 协议层：系统提示词给出精确的 JSON 契约 + 每个工具的参数签名（消灭参数幻觉的源头）
   - 解析层：三级容错解析（整段→围栏→首个配平块）
   - 恢复层：解析失败不终止——原始输出回填历史 + 纠错指令重试；连续失败升级为反思轮
   - 诊断层：所有失败事件携带根因提示与原始响应片段，UI 直接可见
   - 配置层：按模型特性治理输出预算（推理模型给足 max_tokens）
3. **量化验证**：修复前后对照——同一任务 15 步多次失败 → 4 步零错误；规划/反思开启后 6 步任务在第 4 步如期触发复盘
4. **沉淀为通用认知**：推理模型的输出预算管理、工具目录的签名完整性、失败事件的"可诊断性"——这三点适用于所有 LLM Agent 工程

---

## 五、大厂面试官视角的补充

### 面试官大概率会问的问题（附考察点与应答要点）

1. **"为什么不用 LangChain / DeepEval / OpenAI Evals，要自己写？"**
   考察技术选型判断。应答：评测场景需要完全掌控提示词、采样参数与指标口径（Eval 类框架封装过重、行为黑盒）；Agent 场景当时只需要一个 ReAct 循环 + 6 个工具，6000 行内自研可控可测，避免框架升级带来的行为漂移。同时明确承认：如果需要复杂工具生态或 RAG 编排，用框架更合理。
2. **"为什么 Agent 不用 Function Calling 原生协议？"**
   考察兼容性取舍。应答：目标是任意 OpenAI 兼容端点零改造可用（本地 vLLM/Ollama 对 tools 支持参差），文本协议是最大公约数；代价是解析脆弱性，用容错解析+自愈重试+诊断事件补偿。可加一句：如果锁定现代端点，迁移到原生 tools API 是明确的改进方向。
3. **"怎么保证评测结果可信？"**
   考察评测方法论。应答要点：temperature=0 + 固定 seed 保证可复现；指标实现有单元测试；数据集本地化避免线上变动；**坦诚数据污染是行业难题**，本项目数据集是小规模自建样例，若做严肃benchmark需查重与去污染。
4. **"断点续跑的检查点怎么设计的？失败结果缓存问题怎么发现和修的？"**
   考察工程严谨性。用问题 3 的真实案例回答，并主动讲"缓存语义"的教训与改进方案。
5. **"Agent 的沙箱安全怎么做的？能被绕过吗？"**
   考察安全思维。应答：路径钳制（先 resolve 再 relative_to 校验）防穿越、绝对路径/盘符拒绝、命令黑名单 + 60s 超时；**主动承认边界**：`run_command` 本质仍能执行任意 shell 命令（黑名单可被编码绕过），生产化需要_job object/容器级隔离_——主动说出局限是大加分。
6. **"SSE 和 WebSocket 为什么选 SSE？"**
   单向推送场景，SSE 基于纯 HTTP、自动重连、Flask 原生支持、无需额外依赖；不需要双向通道。
7. **"规划/反思真的有收益吗？怎么证明？"**
   考察实验思维。诚实回答：目前是**功能验证**（正确触达、内容合理）而非**消融实验**（开/关对照成功率、步数、token 成本）；正确的做法是固定任务集做 A/B 消融——主动给出实验设计是加分项。
8. **"并发评测怎么处理限流和稳定性？"**
   讲指数退避+全抖动+Retry-After 尊重、按模型并发信号量、检查点使重试代价可控；可结合真实遇到的智谱 429 限流案例。
9. **"长任务的上下文管理？压缩丢信息怎么办？"**
   讲保留头（系统+任务+计划）+ 最近窗口、中段折叠为单行摘要的策略；承认信息损失无量化评估，改进方向是摘要质量评测或检索式记忆。
10. **"如果要多用户/产品化，改哪里？"**
    考察架构演进：鉴权层、任务队列化（现进程内线程→Celery/RQ）、运行注册表外置（Redis）、前端框架化、Agent 工具容器化隔离。

### 加分项（已具备，简历/面试要主动亮出）

- 完整的**根因分析闭环**案例（推理模型 token 预算问题），有对照实验数据
- 显式的**工程取舍**叙事（文本协议 vs 原生 tools、兼容性 vs 可靠性）
- **可测试性**设计（MockModel 离线链路、事件溯源渲染、纯函数压缩可单测）
- 真实量化对照：15 步失败 → 4 步零错误；规划/反思按配置精准触发

### 风险点（会被挑战）与应对

| 风险点 | 面试官可能的质疑 | 应对 |
|---|---|---|
| 规模小（6000 行） | "这也算项目？" | 强调深度而非广度：单条链路打穿到协议层，且有真实排障闭环 |
| 评测集小 | "几个样本能说明什么？" | 承认定位是**框架正确性验证**而非 benchmark 结论；框架与数据集解耦，接入大集只是配置问题 |
| 前端无框架 | "为什么不用 React？" | 单文件交付、零构建链路是特性；承认若继续演进应迁移 Vite+框架，状态管理复杂度已接近阈值 |
| reflection 价值 | "没有消融数据就是玄学" | 主动给出消融实验设计（同任务集×开关矩阵×成功率/步数/token 三指标） |
| 安全性 | "run_command 就是任意命令执行" | 承认并给出生产化路径（容器/Job Object 隔离、allowlist） |

### 建议补充的量化数据（让简历更硬）

- [ ] 评测吞吐数据（如：N 并发下 X 样本/分钟，与串行对比加速比）
- [ ] 断点续跑的恢复成本（中断后重跑只补缺失样本的耗时对比）
- [ ] 规划/反思消融：同任务集开关对照的成功率与平均步数
- [ ] 接入模型数量与端点类型（本地 vLLM、Ollama 各跑通一次即可写"3 类端点"）
- [ ] 历史压缩触发前后 token 消耗对比

---

## 附：一页速记（面试前最后过一遍）

- **一句话**：模型无关的 LLM 评测 + ReAct Agent 框架，6000 行 Python，评测引擎（双执行器/断点续跑/四指标）+ 五合一 Web 交互（SSE 流式/任务会话/可折叠轨迹）
- **最大难点**：不可靠模型驱动结构化协议 → 五层防御纵深（契约/解析/恢复/诊断/预算），15 步失败→4 步零错误
- **三个增强**：规划/反思/记忆压缩，配置+UI 双粒度开关
- **三个主动承认**：文本协议的取舍、reflection 缺消融实验、run_command 需容器级隔离
- **三个量化目标**（补齐后简历更硬）：吞吐加速比、消融成功率、压缩 token 节省
